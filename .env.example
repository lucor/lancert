# lancert.dev configuration
# Copy to .env and adjust values for your deployment.
# CLI flags (e.g. -server-ip) override these.

# Required: public IP of this server (used for DNS A records)
LANCERT_SERVER_IP=

# Listen addresses
LANCERT_DNS_ADDR=:53
LANCERT_HTTP_ADDR=:8443

# Data directory for certs and keys
LANCERT_DATA_DIR=data

# ACME account key path
LANCERT_ACCOUNT_KEY=data/account-key.pem

# Email for Let's Encrypt account (optional)
LANCERT_EMAIL=

# DNS zone (with or without trailing dot)
LANCERT_ZONE=lancert.dev.

# Use Let's Encrypt staging environment
LANCERT_STAGING=false

# Pre-generate certificates for common IPs at startup
LANCERT_PREGEN=false

# Trusted reverse proxy subnet (CIDR, e.g. 172.20.0.0/16).
# When set, real client IPs are extracted from X-Forwarded-For
# only if RemoteAddr falls within this subnet. Leave empty for
# direct connections.
LANCERT_TRUSTED_PROXY=

# Required: secret key for IP hashing (keyed BLAKE2b).
# Hashed IPs are used for rate limiting and logging.
# Use a high-entropy random value (e.g. `openssl rand -hex 32`).
# Changing this invalidates existing rate-limit buckets.
LANCERT_IP_HASH_SALT=
